
//--------------------------------------------------------------    
    //Adding a intermediate node for making bezier curves
    // var peopleById = d3.map(nodes, function(d) { return d.attendee_id; });
    // var reasonsById = d3.map(reasons, function(d) { return d.reason_id; });
    console.log(links);
    
    // links.forEach(function(link) {
    //     var s = link.source = peopleById.get(link.source),
    //         t = link.target.reason_id = reasonsById.get(link.target.reason_id),
    //         i = {}; // intermediate node
    //     //nodes.push(i);
    //     //links.push({source: s, target: i}, {source: i, target: t});
    //     //bilinks.push([s, i, t]);
    //   });
    
      //console.log(links)


      //link.attr("d", positionLink);

//--------------------------------------------------------------


    // line = d3.line()
    //         .x(function(d) { return x(d.source); })
    //         .y(function(d) { return y(d.target); })
    //         .curve(d3.curveCatmullRom.alpha(0.5));

//--------------------------------------------------------------

// pairedUsers.forEach(function(element) {
//     var reqdNode = nodes.filter(node => node.attendee_id ==element);
//     console.log(reqdNode[0].name);
//   });



//--------------------------------------------------------------
